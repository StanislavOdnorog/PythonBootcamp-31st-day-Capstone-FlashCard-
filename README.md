# Flashcard Program

A simple flashcard program built with Python and tkinter.

## Installation

1. Clone the repository.
2. Install dependencies using `pip install -r requirements.txt`.

## Usage

1. Run the program: `python main.py`.
2. Use the buttons to manage and review flashcards.
